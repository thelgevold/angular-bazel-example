package(default_visibility = ["//visibility:public"])

load("@angular//:index.bzl", "ng_module")

ng_module(
  name = "editor",
  srcs = glob(["*.ts"]),
  assets = ["editor.css"],
  tsconfig = "//:tsconfig.json",
)