package(default_visibility = ["//visibility:public"])
load("@angular//:index.bzl", "ng_module")

ng_module(
  name = "grid",
  srcs = glob(["**/*.ts"]),
  tsconfig = "//:tsconfig.json",
  assets = ["grid.html"]
)